# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright E2E output (Sprint 7) — traces/screenshots/HTML report from
# local runs, never meant to be committed.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Environment variables (dev-convenience values only, but keep the pattern
# consistent with backend/.gitignore — .env.example stays tracked)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
